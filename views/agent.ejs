<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= agent.displayName %> - Valorant Agent
    </title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css">
    <style>
        .agent-card {
            background-color: rgba(0, 0, 0, 0.7);
            border: 1px solid rgba(255, 255, 255, 0.1);
            transition: transform 0.2s ease;
        }

        .agent-card:hover {
            transform: scale(1.05);
        }

        .ability-icon {
            width: 40px;
            height: 40px;
        }

        .role-description {
            background-color: rgba(0, 0, 0, 0.7);
            border: 1px solid rgba(255, 255, 255, 0.1);
            padding: 1rem;
            border-radius: 0.5rem;
        }
    </style>
</head>

<body class="bg-black text-white">
    <div class="container mx-auto p-8">
        <div class="bg-gray-800 p-8 rounded-lg shadow-lg">
            <div class="flex items-center justify-between">
                <img src="<%= agent.fullPortrait %>" alt="<%= agent.displayName %>" class="w-1/3 rounded-lg">
                <div class="ml-8">
                    <h1 class="text-5xl font-bold mb-4">
                        <%= agent.displayName %>
                    </h1>
                    <p class="text-lg mb-4">
                        <%= agent.description %>
                    </p>
                    <p class="text-2xl font-semibold mb-2">Role: <%= agent.role.displayName %>
                    </p>
                </div>
            </div>
            <div class="mt-8">
                <h2 class="text-3xl font-bold mb-4">Abilities</h2>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                    <% agent.abilities.forEach(ability=> { %>
                        <div class="flex items-center p-4 bg-gray-900 rounded-lg">
                            <img src="<%= ability.displayIcon %>" alt="<%= ability.displayName %>"
                                class="ability-icon mr-4">
                            <div>
                                <h3 class="text-xl font-bold">
                                    <%= ability.displayName %> (<%= ability.slot %>)
                                </h3>
                                <p class="text-sm">
                                    <%= ability.description %>
                                </p>
                            </div>
                        </div>
                        <% }); %>
                </div>
            </div>
            <div class="mt-8 role-description">
                <div class="flex items-center mb-4">
                    <img src="<%= agent.role.displayIcon %>" alt="<%= agent.role.displayName %>" class="w-12 h-12 mr-4">
                    <h2 class="text-2xl font-bold">
                        <%= agent.role.displayName %>
                    </h2>
                </div>
                <p class="text-lg">
                    <% if (agent.role.displayName==='Duelist' ) { %>
                        Duelists are self-sufficient fraggers who, through their abilities and skills, seek out
                        engagements and typically do most of the fighting for their team.
                        <% } else if (agent.role.displayName==='Initiator' ) { %>
                            Initiators challenge angles by setting up their team to enter contested ground and push
                            defenders away.
                            <% } else if (agent.role.displayName==='Controller' ) { %>
                                Controllers are experts in slicing up dangerous territory to set their team up for
                                success.
                                <% } else if (agent.role.displayName==='Sentinel' ) { %>
                                    Sentinels are defensive experts who can lock down areas and protect their teammates
                                    from flanks.
                                    <% } else { %>
                                        This role is not defined in the documentation.
                                        <% } %>
                </p>
            </div>
        </div>
    </div>
    <%- include('partials/footer.ejs') %>
</body>

</html>